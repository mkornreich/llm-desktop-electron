{
  "name": "claude-desktop-electron-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Runs the unpacked Claude Desktop app.asar under a stock Electron runtime.",
  "scripts": {
    "start": "electron app",
    "test": "node --test openai-proxy/*.test.mjs settings/config.test.js scripts/*.test.mjs"
  },
  "devDependencies": {
    "classic-level": "^3.0.0",
    "electron": "43.2.0"
  }
}
